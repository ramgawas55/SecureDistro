# CLI

Command-line client for SentinelOS-Lite.

## Build

```
npm install
npm run build
```

## Configure

Create `~/.sentinelos/config.json`:

```
{
  "url": "http://localhost:4000",
  "token": ""
}
```
